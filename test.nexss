
config.json // Reads config to the memory
// config.json is parsed so you can use there variables etc, then you can load another json(s) and use
// variables from the previous one etc.

// Use of Storage/JSON package which basically reads values from the json file.
// by default it is nexssStorage.json
// more: nexss Storage/JSON help
Storage/JSON/Get emailLastUID --nxsDefault="1"
// Email/Download --emailSearch="UNSEEN SINCE ${lastCheck}" (to eml)

Email --emailSearch=${emailLastUID} --attachmentType="pdf" --attachmentType="jpg"

Convert/ToTxt --nxsInFrom=emailStoredAttachments
// Cleanup
$# --nxsDelete=emailStoredAttachments,emailFound,nxsInfo,searchCriteria

FS/Find Invoice Faktura --inPath="${cwd}/invoices" --inTypes=txt
// Replace Stream you can also use: nexssFileExt

Data/Replace --replaceFrom=".txt" --replaceTo=".pdf"
Printer // Print with any language - ahk?

// Storage/JSON/Set emailLastUID // As all is done and set the emailLastUID